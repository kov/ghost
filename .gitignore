# Rust build output — any crate's target dir, at any depth (not just the root
# workspace one), so a stray per-crate target never gets committed again.
target/

# Scratch crates and spikes kept beside the workspace, not part of it.
/experiments

# Screen captures, recordings and logs dropped at the root while working.
/*.png
/*.log
/*.cast
